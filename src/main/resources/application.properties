# 1. DATABASE CONNECTION
# Discrepancy Fix: Added SSL mode and fallback logic
spring.datasource.url=${postgresql://ap13_user:xC6Dt60v8GKntbLTcbOZpL3cHnZPb5zO@dpg-d7i7ss67r5hc73c5n17g-a.virginia-postgres.render.com/ap13}
spring.datasource.username=${ap13_user}
spring.datasource.password=${xC6Dt60v8GKntbLTcbOZpL3cHnZPb5zO}
spring.datasource.driver-class-name=org.postgresql.Driver

# 2. HIBERNATE & JPA
# Discrepancy Fix: Explicitly set Postgres Dialect to prevent auto-detection failure
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# 3. CONNECTION POOLING (Critical for Cloud)
# Discrepancy Fix: This prevents the app from crashing if the DB takes 2 seconds to wake up
spring.jpa.defer-datasource-initialization=true
spring.datasource.hikari.initialization-fail-timeout=0
spring.datasource.hikari.maximum-pool-size=5

# 4. RENDER PORT
server.port=${PORT:5432}